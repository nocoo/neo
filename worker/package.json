{
  "name": "neo-worker",
  "version": "0.2.3",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.10.0"
  }
}
